[package]
name = "devdrop"
version = "0.1.1"
edition = "2024"

[profile.release]
lto = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
toml = "1.1.2"
